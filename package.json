{
  "name": "kofi-discord-notification",
  "version": "1.0.0",
  "description": "Netlify function to forward Ko-fi webhook donations to Discord and optionally archive supporters in a GitHub Gist.",
  "main": "netlify/functions/kofi.js",
  "scripts": {
    "dev": "netlify dev",
    "start": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@octokit/core": "^4.2.0",
    "discord-webhook-node": "^1.1.8"
  },
  "devDependencies": {
    "netlify-cli": "^14.0.0"
  }
}
